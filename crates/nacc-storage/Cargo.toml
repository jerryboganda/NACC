[package]
name = "nacc-storage"
description = "SQLite persistence and versioned migrations for durable NACC state (master plan S4.4/S16 -- Phase 2 scope for real schema and migrations)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
nacc-domain = { workspace = true }
# Phase 4: provider installation and capability-snapshot records are
# persisted as the provider-core types themselves (see `providers.rs`'s
# module doc for why this direction and not a duplicated storage-local
# shape). Acyclic: nacc-provider-core never mentions storage.
nacc-provider-core = { workspace = true }
nacc-events = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
tokio = { workspace = true }
rusqlite = { workspace = true }
rusqlite_migration = { workspace = true }
